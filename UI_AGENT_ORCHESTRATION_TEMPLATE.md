# UI Agent Orchestration Template

## 适用场景
用于需要高质量 UI 系统落地的任务：新页面、新工作台、重设计、交互流程补齐、浏览器验收和视觉一致性修正。

默认调度方式：
- GPT-5.5：主调度、产品/设计判断、任务拆分、冲突收敛、最终验收。
- Build Web Apps：生成或固化 UI 视觉方向，约束实现质量、交互完整性和响应式要求。
- Next.js 技术栈：后续产品化前端默认落在 `frontend/`，采用 Next.js App Router + React + TypeScript + Tailwind CSS。
- Browser Use：运行本地页面，做真实浏览器检查、截图、交互验证和控制台错误检查。
- GPT-5.3 Sub-Agents：按边界清晰的模块执行代码编写、测试补齐或局部修复。

## 主任务提示词模板
```text
当前任务：<一句话说明要完成的 UI 系统目标>

主调度模型：GPT-5.5
执行约束：
1. 先读 CONTEXT.md、README.md、ARCHITECTURE.md，以及与 UI 相关的设计文档。
2. 使用 Build Web Apps 的思路建立 UI 实现契约：目标用户、页面结构、核心流程、视觉基准、交互状态、响应式要求。
3. 如果是产品化前端任务，默认在 `frontend/` 使用 Next.js App Router + React + TypeScript + Tailwind CSS；`ui/app.py` 只作为 Streamlit 研究原型保留。
4. 将编码任务拆给 GPT-5.3 Sub-Agents。每个 Sub-Agent 必须有独立写入范围，不能互相覆盖。
5. 主线程保留架构判断、任务合并、冲突处理、浏览器验收和最终交付。
6. 使用 Browser Use 验证本地 UI：页面加载、核心交互、控制台错误、桌面/移动视口、文本溢出和视觉一致性。
7. 完成后更新 CONTEXT.md；如果运行方式、目录结构或架构边界变化，同步更新 README.md / ARCHITECTURE.md。

验收标准：
- 页面能启动并在浏览器中正常加载。
- 核心用户流程可点击完成，关键控件不是静态摆设。
- 桌面和移动视口无明显重叠、溢出、遮挡。
- 控制台无新增关键错误。
- 代码边界清晰，没有无关重构。
- 文档记录当前进度、关键决定、阻塞和下一步。
```

## Sub-Agent 拆分模板
每个 Sub-Agent 的任务必须小而完整，写入范围尽量互斥。

```text
你是 GPT-5.3 Sub-Agent，负责本次 UI 系统任务中的一个独立编码分片。

主任务目标：
<复制主任务目标>

你的职责：
<例如：实现导航与页面骨架 / 实现 Agent 挖因子工作台核心交互 / 补齐数据 mock 与状态流 / 修复响应式样式 / 增加最小测试>

写入范围：
- <允许修改的文件或目录>

不得修改：
- <其他 Sub-Agent 或主线程负责的文件>
- <项目文档，除非明确要求>

上下文要求：
1. 先读 CONTEXT.md 和与你负责文件直接相关的代码。
2. 复用现有组件、样式、数据结构和启动方式。
3. 不引入无关依赖，不做无关重构。
4. 如果发现需求冲突或依赖缺失，在最终回复中明确说明，不自行扩大范围。

交付要求：
- 直接在工作区完成代码修改。
- 最终列出修改文件、实现内容、已运行的验证命令、未验证项和风险。
```

## 推荐拆分方式
| 分片 | GPT-5.3 职责 | 建议写入范围 | 主线程验收重点 |
|---|---|---|---|
| UI 骨架 | 页面路由、布局、导航、全局视觉变量 | `frontend/src/app/`、`frontend/src/components/shell/` | 是否贴合设计基准，是否有清晰信息架构 |
| 核心交互 | 任务拆解、候选因子、审核状态、流程推进 | `frontend/src/components/agent-workbench/` | 控件是否真的驱动状态变化 |
| 数据接口 | mock 数据、DuckDB/API 读取、状态适配 | `frontend/src/lib/`、Python API 层 | 字段口径是否与后端一致 |
| 评估展示 | 图表、表格、判定标签、空状态 | 评估相关组件 | 空数据和错误状态是否可解释 |
| 响应式与 polish | 移动端、溢出、可访问性、视觉细节 | 样式文件和局部组件 | 桌面/移动是否稳定，无遮挡 |
| 测试与脚本 | 最小单测、启动脚本、验收脚本 | `scripts/`、测试目录 | 命令是否可复现，失败信息是否清楚 |

## GPT-5.5 主线程执行清单
1. 读取上下文，确认本次任务的 UI 基准和技术边界。
2. 写出页面/模块拆分，标注每个分片的写入范围。
3. 并行发起 GPT-5.3 Sub-Agents，避免多个 agent 修改同一文件。
4. 主线程处理无法拆分的关键路径，例如架构胶水、公共类型和最终合并。
5. 收回 Sub-Agent 结果后逐项 code review，不接受越界修改。
6. 启动本地 UI，使用 Browser Use 做真实浏览器验收。
7. 根据浏览器结果修复视觉、交互、响应式和控制台问题。
8. 更新文档并给出最终交付说明。

## Browser Use 验收脚本清单
浏览器验收至少覆盖：
- 打开本地地址；Next.js 前端默认是 `http://127.0.0.1:3000`，Streamlit 原型是 `http://127.0.0.1:8501`。
- 检查首屏是否加载完成，主要导航和标题是否可见。
- 点击核心流程：目标输入、任务生成、候选选择、审核、结果展示。
- 检查控制台错误。
- 检查桌面视口和移动视口是否存在文字溢出、组件重叠、按钮不可点。
- 如果有设计图或视觉基准，对照关键布局、颜色、字体、组件密度和状态标签。

## 最终交付格式
```text
改动：
- <主要代码/文档改动>

验证：
- <启动命令>
- <Browser Use 验收结果>
- <测试命令>

剩余风险：
- <未覆盖项或阻塞>

下一步：
- <最合理的一个或两个后续动作>
```
